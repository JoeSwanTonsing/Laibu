import React from 'react';
import {
  SafeAreaView,
  StyleSheet,
  ScrollView,
  View,
  Text,
  StatusBar,
} from 'react-native';

const App = () => {
  return (
    <SafeAreaView style={styles.sav}>
      <Text> Hello App</Text>
    </SafeAreaView>
  );
};

const styles = {
  sav: {},
};

export default App;
